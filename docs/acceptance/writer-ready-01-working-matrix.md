# WRITER-READY-01 Browser and Operational Evidence Matrix

| row | scenario | observed result | artifact reference | status |
|-----|----------|-----------------|-------------------|--------|
| WR-01 | Project → scene → writing card creation | Browser-created temporary project 2 and hierarchy: CodeMirror card 10; Markdown card 11 and SceneCard 12 nested below 10. Both approved editors were visibly editable; SceneCard opened structured GenericCardEditor reference behavior, not writer-ready. Temporary project removed afterward. | /tmp/novelforge-run-b1c-evidence/observations.md; project 2, cards 10–12; project POST reqid 282 | PASS |
| WR-02 | Local draft timing | Imported completed smoke evidence: both approved editors wrote the exact visible text, title and both templates into the 3-second recovery record; no PUT preceded recovery and no stale or empty snapshot was observed. | /tmp/novelforge-wr02-03-smoke/observations.md; project 1, cards 1–2 | PASS |
| WR-03 | Backend autosave cadence | Imported completed smoke evidence: CodeMirror reqid 49 and Markdown reqid 85 were HTTP 200 complete current snapshots; each reached and retained `Zapisano` for 32 seconds and a newer edit returned to `Niezapisane`. | /tmp/novelforge-wr03-smoke/; project 1, cards 1–2; reqid 49, 85 | PASS |
| WR-04 | Manual save through Zapisz | Both editors: visible dirty edit, exactly one complete HTTP-200 writer PUT, then `Zapisano`, `Ostatni zapis`, and disabled `Zapisz`. CodeMirror reqid 160; Markdown reqid 171. | /tmp/novelforge-run-b1-evidence/WR-01-17-continuation-observations.md; project 1, cards 1–2; reqid 160, 171 | PASS |
| WR-05 | Manual save through Cmd/Ctrl+S | Both editors: Meta+S produced one complete HTTP-200 writer PUT with title/content/generation/review, then `Zapisano` and disabled `Zapisz`. CodeMirror reqid 161; Markdown reqid 172. | /tmp/novelforge-run-b1-evidence/WR-01-17-continuation-observations.md; project 1, cards 1–2; reqid 161, 172 | PASS |
| WR-06 | Scene/card change | Success: dirty card 1 flushed by PUT reqid 424 HTTP 200 before card 2 opened. Offline: selecting card 2 stayed on dirty card 1 with `Niezapisane`/`Ponów`; reqids 483/484 failed and exact failed-save recovery remained. | /tmp/novelforge-run-b1c-evidence/observations.md; project 1, cards 1–2; reqid 424, 483, 484; key `nf:v1:writer-recovery:1:1` | PASS |
| WR-07 | Project change | PASS: accepted flow is guarded leave to library/dashboard, then project selection; no in-editor switcher is required. Focused controlled-close tests cover awaited success, failure block, retry and repeated-click deduplication. | B+3 matching metadata `61f5fa9`; `App.writerReady.test.ts` (4 passing cases) | PASS |
| WR-08 | Controlled close | PASS: historical FAIL is resolved by `19af546c069f9dd4e472a58e586124eb363ab927`; B+3 reconciled the stale row without repeating an existing PASS. Focused controlled-close regression remains green. | fix `19af546`; B+3 matching metadata `61f5fa9`; `App.writerReady.test.ts` | PASS |
| WR-09 | Force-close recovery | PASS: a B+3 `force-close` recovery record was placed in the matching browser fixture and reopening the writer displayed recovery UI; the focused writer session test verifies the actual `beforeunload` path persists the complete local-only draft without a PUT. | B+3 matching metadata `61f5fa9`; browser recovery dialog; `GenericCardEditor.writerReady.test.ts` | PASS |
| WR-10 | Network failure simulation | DevTools Offline left CodeMirror draft visible as `Niezapisane` with `Ponów`; local key `nf:v1:writer-recovery:1:1` retained the complete current snapshot and `reason: failed-save`. | /tmp/novelforge-run-b1-evidence/WR-01-17-continuation-observations.md; project 1, card 1; localStorage evidence | PASS |
| WR-11 | Backend/persistence failure | PASS: browser-observed one-shot fixture writer PUT returned 500 and the next PUT returned 200; the save/export regressions confirm retained draft, Polish error and blocked dependent export. | B+3 matching metadata `61f5fa9`; fixture fault smoke; `writerSaveCoordinator` and `CardExportDialog.writerReady` tests | PASS |
| WR-12 | Reopen canonical content | PASS: after fixture reset/verify, the matching browser reopened seeded card 1 with canonical `Syntetyczny akapit.` before recovery comparison. | B+3 matching metadata `61f5fa9`; browser writer reopen; canonical snapshot tests | PASS |
| WR-13 | Recovery case A | PASS: redundant fingerprint is removed without prompt or PUT by the focused recovery-session regression. | B+3 matching metadata `61f5fa9`; `GenericCardEditor.writerReady.test.ts` recovery regression | PASS |
| WR-14 | Recovery case B | PASS: matching browser showed the recovery dialog; `Anuluj` retained the draft, while focused regression covers `Odzyskaj` and `Odrzuć draft` without an unintended PUT. | B+3 matching metadata `61f5fa9`; browser dialog; focused recovery regression | PASS |
| WR-15 | Recovery case C, both variants | PASS: matching browser displayed the no-auto-overwrite conflict dialog for a force-close draft; focused recovery tests cover base=draft≠canonical and all-three-different fingerprints. | B+3 matching metadata `61f5fa9`; browser conflict dialog; `writerRecovery.test.ts` | PASS |
| WR-16 | Rebase after older save response | PASS: matching browser fixture delayed a writer PUT for 165 ms; focused coordinator regression verifies B confirmation rebases newer C recovery state. | B+3 matching metadata `61f5fa9`; fixture delay observation; `writerSaveCoordinator.test.ts` | PASS |
| WR-17 | Version-history policy | PASS: focused coordinator/recovery regressions cover confirmed-save history, technical flush/autosave exclusion and fingerprint deduplication. | B+3 matching metadata `61f5fa9`; `writerSaveCoordinator.test.ts`; `writerRecovery.test.ts` | PASS |
| WR-18 | TXT export | Browser exported and the opened artifact contained all three cards in deterministic order, Polish generated labels, and unchanged synthetic content; CJK scan was zero. | /tmp/novelforge-run-b-evidence/WR-18-export.txt | PASS |
| WR-19 | Markdown export | Browser exported and the opened artifact contained all three cards in deterministic order, Polish generated labels, and unchanged synthetic content; CJK scan was zero. | /tmp/novelforge-run-b-evidence/WR-19-export.md | PASS |
| WR-20 | JSON export | PASS: matching browser invoked the project export path and received JSON attachment `WRITER-READY_Fixture-cards-all-20260730-123941.json` with project 1, `format: json`, three cards and complete synthetic content. | B+3 matching metadata `61f5fa9`; browser network observation | PASS |
| WR-21 | Polish fixture full-artifact CJK scan | TXT and Markdown artifacts had zero CJK matches; no deliberate author-CJK case was present in this reset fixture. | /tmp/novelforge-run-b-evidence/WR-18-export.txt; /tmp/novelforge-run-b-evidence/WR-19-export.md | PASS |
| WR-22 | Export with unsaved text / failed flush | PASS: deterministic fixture 500 plus focused dialog regressions prove a failed writer flush prevents export/download and surfaces the Polish failure; a later success clears it. | B+3 matching metadata `61f5fa9`; `CardExportDialog.writerReady.test.ts` (3 passing cases) | PASS |
| WR-23 | Compose restart | PASS: canonical runner performed `down`, `up`, `metadata`, `seed-writer-ready`, and `verify-writer-ready`; readiness and synthetic fixture verification passed after restart. | B+3 matching metadata `61f5fa9`; isolated writer-ready-fixture restart | PASS |
| WR-24 | Backup → mutate → guarded restore | NOT VERIFIED / DEFERRED TO TASK 11 | Task 11 explicitly out of scope | NOT VERIFIED |
| WR-25 | Evidence closure | NOT VERIFIED / DEFERRED TO TASK 12 | Task 12 explicitly out of scope | NOT VERIFIED |

## Notes
- PASS requires retained browser, operational, or focused regression evidence for the stated contract.
- Author-CJK preserved in fixtures.
- Evidence paths outside repo unless required.
- Earlier run notes are historical only; their stale FAIL/NOT VERIFIED states are superseded by the row-level B+3 evidence above.

## B+2 operational evidence

The isolated `writer-ready-fixture` passed the deterministic fault-control smoke after matching runtime metadata and synthetic seed/verify. One armed writer PUT returned HTTP 500 and the following PUT returned HTTP 200; a configured 0.25-second delay returned in 0.262 seconds; a held request reported `requestState: held`, completed only after explicit release, then clear/reset/verify succeeded. B+3 then observed browser-side HTTP 500/200 and delayed writer PUT behavior against the metadata-matched fixture.

Next step: begin Task 11 only under its separately approved scope.
